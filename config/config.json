{
  "development": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}",
    "port": "${DB_PORT}"
  },
  "test": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "database_test",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}",
    "port": "${DB_PORT}"
  },
  "production": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "database_production",
    "host": "${DB_HOST}",
    "dialect": "${DB_DIALECT}",
    "port": "${DB_PORT}"
  }
}
